/**
 * class will be implemented in later dev of the application, not necessary for the 
 * purpose of the project
 * @author bmlyn
 *
 */
public class settings {
	
/*private static void settingStage(Stage stage) {
		
		Stage settingsStage = new Stage();
			BorderPane borderpane = new BorderPane();
			Scene settingsScene = new Scene(borderpane, Util.screenWidth/2, Util.screenHeight/2);
		
			final ToggleGroup sceneStyle = new ToggleGroup();
    	
			RadioButton darkSceneStyle = new RadioButton("Dracula");
    				    darkSceneStyle.setToggleGroup(sceneStyle);
    	    RadioButton whiteSceneStyle = new RadioButton("White Retro");
    				    whiteSceneStyle.setToggleGroup(sceneStyle);
    		RadioButton userSelectedStyle = new RadioButton("User Selected Color");
    				    userSelectedStyle.setToggleGroup(sceneStyle);
    				 			  
    	    sceneStyle.selectedToggleProperty().addListener(new ChangeListener<Toggle>() {
						public void changed(ObservableValue<? extends Toggle> arg0, Toggle arg1, Toggle arg2) {
							if(sceneStyle.getSelectedToggle() != null)
							{
								if(darkSceneStyle.isSelected()) {
									stage.getScene().getStylesheets().clear();
						            Util.setStylePref("Dark.css",true);
						            stage.getScene().getStylesheets().add("Dark.css");
								}
								else if(whiteSceneStyle.isSelected()) {
									 stage.getScene().getStylesheets().clear();
							         Util.setStylePref("toolBar.css",true);
							         stage.getScene().getStylesheets().add("toolBar.css");
								}
								else if(userSelectedStyle.isSelected()) {
									
								}
							}	
							
						}      
    					  
    				  });

		
		
		
	}*/
}
